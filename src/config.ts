// The demo reads production, so its public URL shows what a tenant actually gets.
export const API_BASE_URL = "https://translation.retailsvc.com/api/v1";

// This app's own module. Deliberately distinct from the `demo` module owned by
// hiiretail-translation-service, and published by this repo's own pipeline.
export const MODULE_ID = "trs-demo-app";

export const DEFAULT_LANG_TAG = "en-US";
